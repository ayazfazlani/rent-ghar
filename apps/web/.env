# API URL Configuration
# For production (live), uncomment and update the URL:
NEXT_PUBLIC_API_URL=https://pro.adca.pk

PORT=3010

# For development (local), uncomment and update the URL:
# NEXT_PUBLIC_API_URL=http://localhost:3005

# Note: If no URL is provided, the app will use relative paths (/api),
# which works if the API is on the same domain as the frontend.